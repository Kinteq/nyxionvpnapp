# 🚀 Nyxion VPN Bot - Полное руководство по развертыванию

## 📋 Содержание
1. [Фаза 1: Улучшения текущего бота](#фаза-1-улучшения-текущего-бота) ✅ ГОТОВО
2. [Фаза 2: PostgreSQL миграция](#фаза-2-postgresql-миграция)
3. [Фаза 3: Mini App на Next.js](#фаза-3-mini-app-на-nextjs)
4. [Фаза 4: Мультисервер архитектура](#фаза-4-мультисервер-архитектура)
5. [Фаза 5: Интеграция ЮКасса](#фаза-5-интеграция-юкасса)

---

## ✅ Фаза 1: Улучшения текущего бота (ГОТОВО)

### Что реализовано:
- ✅ QR-коды для оплаты
- ✅ Polling проверка оплаты (каждые 15 сек)
- ✅ Таймер ожидания оплаты с уведомлениями
- ✅ Напоминания каждые 5 минут
- ✅ Кнопка "Проверить оплату" для ручной проверки
- ✅ Автоматическая очистка истекших платежей

### Как протестировать:
1. Запустите бота: `python3 histeriabot.py`
2. Выберите "Купить VPN" → выберите валюту
3. Получите QR-код и ссылку на оплату
4. Бот будет проверять оплату автоматически

---

## 🗄️ Фаза 2: PostgreSQL миграция

### Шаг 1: Установка PostgreSQL на VPS

```bash
# Подключитесь к вашему VPS по SSH
ssh root@your_vps_ip

# Установка PostgreSQL (Ubuntu/Debian)
sudo apt update
sudo apt install postgresql postgresql-contrib -y

# Запуск PostgreSQL
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### Шаг 2: Создание базы данных и пользователя

```bash
# Переключитесь на пользователя postgres
sudo -i -u postgres

# Создайте пользователя БД
createuser nyxion_vpn --pwprompt
# Введите пароль (сохраните его!)

# Создайте базу данных
createdb nyxion_vpn -O nyxion_vpn

# Выход
exit
```

### Шаг 3: Настройка доступа (если БД на VPS)

Отредактируйте `/etc/postgresql/*/main/pg_hba.conf`:
```bash
sudo nano /etc/postgresql/*/main/pg_hba.conf
```

Добавьте строку (замените YOUR_MAC_IP):
```
host    nyxion_vpn    nyxion_vpn    YOUR_MAC_IP/32    md5
```

Отредактируйте `/etc/postgresql/*/main/postgresql.conf`:
```bash
sudo nano /etc/postgresql/*/main/postgresql.conf
```

Найдите и измените:
```
listen_addresses = '*'
```

Перезапустите PostgreSQL:
```bash
sudo systemctl restart postgresql
```

### Шаг 4: Настройка на вашем Mac

Создайте файл `.env` в папке бота:
```bash
cd "/Users/nevermore/Desktop/Nyxion_VPN_bot/Ver 2/ver2.2"
nano .env
```

Добавьте:
```env
# PostgreSQL (если на VPS)
DATABASE_URL=postgresql+asyncpg://nyxion_vpn:ВАШ_ПАРОЛЬ@VPS_IP:5432/nyxion_vpn

# Или локально (для разработки)
# DATABASE_URL=postgresql+asyncpg://nyxion_vpn:ВАШ_ПАРОЛЬ@localhost:5432/nyxion_vpn

# Telegram Bot
TELEGRAM_BOT_TOKEN=8011671501:AAH9nxyd-pXbPHDqc2lyl5LUiYEz-mg875I
ADMIN_ID=1474669885

# Crypto Pay
CRYPTO_PAY_API_TOKEN=513647:AAv2qN58YYe5pKqg2LFYFCE2sS6JKp6DcQT

# Blitz Panel (основной сервер)
BLITZ_API_URL=https://histeria.nyxionvpn.ru/e2f21bef357f7e53116016ee1e679380
BLITZ_API_TOKEN=2b302927e686f481c1b1e67032ebddec6495110646c8c14a9c81d72bfebbc0e2

# Ngrok
NGROK_AUTHTOKEN=384IPiiT7SXZ7jDmNocTkSLbsTH_2ymBTPZA1EhLVy1rg1iyd
NGROK_DOMAIN=undiscouragingly-validatory-myrtie.ngrok-free.dev
```

### Шаг 5: Установка зависимостей

```bash
pip3 install -r requirements.txt
```

### Шаг 6: Миграция данных

Я создам скрипт миграции, который перенесет данные из `subscriptions.json` в PostgreSQL.

---

## 🎨 Фаза 3: Mini App на Next.js + Vercel

### Цветовая схема (из вашего логотипа):
```css
:root {
  --coral: #FF9A8B;
  --peach: #FFC3A0;
  --blue-gray: #7F9FB5;
  --navy: #1E3A5F;
  --gradient: linear-gradient(135deg, #FF9A8B 0%, #FFC3A0 50%, #7F9FB5 100%);
}
```

### Структура Mini App:

```
nyxion-mini-app/
├── app/
│   ├── page.tsx         # Главная страница
│   ├── profile/         # Профиль пользователя
│   ├── buy/             # Покупка VPN
│   ├── keys/            # Мои ключи
│   └── layout.tsx
├── components/
│   ├── Header.tsx
│   ├── VPNCard.tsx
│   ├── PaymentModal.tsx
│   └── QRCode.tsx
├── lib/
│   ├── telegram.ts      # Telegram Web App API
│   └── api.ts           # API клиент
├── public/
│   └── logo.png
└── package.json
```

### Что нужно с вашей стороны:

1. **Создайте аккаунт на Vercel** (бесплатно):
   - Перейдите на https://vercel.com
   - Войдите через GitHub
   - Дайте мне знать, когда готово

2. **Создайте Mini App в BotFather**:
   ```
   /newapp
   @nyxionvpn_bot
   Название: Nyxion VPN
   Описание: Быстрый и безопасный VPN
   Фото: (загрузите ваш логотип)
   GIF: (можно пропустить)
   ```
   
3. После деплоя на Vercel получите URL и укажите его в BotFather

---

## 🌍 Фаза 4: Мультисервер архитектура

### Концепция:
- Каждый сервер хранится в таблице `servers`
- При создании VPN выбирается сервер с наименьшей нагрузкой
- Пользователь может переключаться между серверами

### Как добавить новый сервер:

```python
# Через админ команду /add_server или напрямую в БД
# Пример:
server_data = {
    "name": "USA Server",
    "country_code": "US",
    "api_url": "https://usa.nyxionvpn.ru/panel_path",
    "api_token": "your_blitz_token",
    "max_users": 1000,
    "priority": 1
}
```

---

## 💳 Фаза 5: Интеграция ЮКасса

### Будет добавлено позже. Потребуется:
1. Регистрация в ЮКасса
2. Получение API ключей
3. Интеграция с webhook

---

## 🔧 Текущий статус

### Готово ✅:
- QR-коды для платежей
- Polling проверка оплаты
- Таймеры и уведомления
- Синхронизация с Blitz панелью
- Структура БД для PostgreSQL
- План архитектуры Mini App

### В процессе 🔄:
- Миграция на PostgreSQL (ждем настройку БД)
- Создание Mini App (ждем Vercel setup)

### Запланировано 📅:
- Redis для кэширования
- Мультисервер поддержка
- ЮКасса интеграция
- Docker контейнеризация

---

## ❓ Следующие шаги

**Ответьте на вопросы:**

1. **PostgreSQL**: Хотите развернуть на VPS или локально для разработки?
2. **IP адрес VPS**: Какой IP вашего VPS для настройки PostgreSQL?
3. **Vercel**: Готовы создать аккаунт?
4. **Приоритет**: Что делаем первым - PostgreSQL или Mini App?

Жду ваших ответов для продолжения! 🚀
